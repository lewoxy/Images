{
  "name": "hotel-hektik",
  "private": true,
  "version": "0.1.0",
  "description": "Hotel Hektik – ein Arcade-Idle-Hotelspiel (Three.js + TypeScript), mechanisch nach der Spezifikation v5 nachgebaut",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "build:single": "tsc --noEmit && vite build --mode single && node scripts/inline-single.mjs",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.ts"
  },
  "dependencies": {
    "three": "^0.186.1"
  },
  "devDependencies": {
    "@fontsource/fredoka": "^5.3.0",
    "@fontsource/lilita-one": "^5.3.0",
    "@types/three": "^0.186.0",
    "typescript": "~5.9.3",
    "vite": "^8.3.1"
  }
}
